#include "subjects.h"
#include "quiz.h"
#include <vector>
using namespace std;

void mathematics() {
    vector<Question> questions = {
      
    };
    RunQuiz(questions);
}

void bulgarianLanguage() {
   vector<Question> questions = {
      
    };
    RunQuiz(questions);
}

void history() {
    vector<Question> questions = {
       
    };
    RunQuiz(questions);
}

void geography() {
    vector<Question> questions = {
       
    };
    RunQuiz(questions);
}

void biology() {
    vector<Question> questions = {
       
    };
    RunQuiz(questions);
}