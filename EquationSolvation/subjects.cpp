#include "subjects.h"
#include "quiz.h"
#include <vector>

void mathematics() {
    std::vector<Question> questions = {
      
    };
    RunQuiz(questions);
}

void bulgarianLanguage() {
    std::vector<Question> questions = {
      
    };
    RunQuiz(questions);
}

void history() {
    std::vector<Question> questions = {
       
    };
    RunQuiz(questions);
}

void geography() {
    std::vector<Question> questions = {
       
    };
    RunQuiz(questions);
}

void biology() {
    std::vector<Question> questions = {
       
    };
    RunQuiz(questions);
}